# Dependencies & build
node_modules/
dist/
reports/

# Env and local secrets (use .env.example as template)
.env
.env.local
.env.*.local

# Regenerated by Figma/Make with real keys — use .env.local instead
utils/supabase/info.tsx

# Supabase CLI local cache (may contain project ref / DB URLs)
supabase/.temp/

# Editor / IDE debug logs
.cursor/debug*.log

# Local thesis / paper — never pushed (see thesis-local/README.md). Not part of Vite dist/.
thesis-local/*
!thesis-local/README.md

# Obsidian local editor state (window layout, plugin config) — not project code
docs/.obsidian/

.DS_Store
